{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm --prefix ../frontend install && npm --prefix ../frontend run build",
    "postinstall": "npm --prefix ../frontend install --silent && npm --prefix ../frontend run build --silent",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Backend server for FixMate using Express, Firebase Admin, and OpenAI APIs",
  "dependencies": {
    "axios": "^1.12.2",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "firebase-admin": "^13.5.0",
    "jsonwebtoken": "^9.0.2",
    "openai": "^5.20.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.2.4",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}

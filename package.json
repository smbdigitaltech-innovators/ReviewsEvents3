{
  "name": "reviewsevents3-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --omit=dev && npm run build && cd ../backend && npm install --omit=dev",
    "start": "cd backend && npm install --omit=dev && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
